= form_for @writer do |f|
  -if @writer.errors.any?
    #error_explanation
      %h2= "#{pluralize(@writer.errors.count, "error")} prohibited this writer from being saved:"
      %ul
        - @writer.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :email
    = f.text_field :email
  .actions
    = f.submit 'Save'
